package cloud;
import cloud.util.CloudUtil;

public class VirtualMachine 
{
    public VirtualMachine()
    {
        /* 1. Inicialice los atributos existentes en las variables de instancia. */
        String name, guestOS;
        long memoryGB;
        int cpuCount;
        
        /* 2. Genere el atributo id de la VirtualMachine en base al método de la 
        clase CloudUtil.getUUID. */
    }
    /* 3. Actualizar los modificadores de acceso si es necesario. */
}
