package cloud;
import cloud.util.CloudUtil;

public class Main 
{
    public static void main(String[] args)
    {
        /* 1. Crear un objeto vm1 de tipo VirtualMachine con variables obtenidas de 
        args que sigan el orden: "name" "guestOs" "memoryGB" "cpuCount". */
        VirtualMachine vm1 = new VirtualMachine();
        
        
        /* 2. Asignar a la variable de instancia nics del objeto vm1 dos objetos de 
        tipo NetworkCard. */
        
        /* 3. Imprimir las variables de instancia de vm1 incluyendo nics de tipo 
        NetworkCard[].*/
        
        CloudUtil cu = new CloudUtil();
    }
}
